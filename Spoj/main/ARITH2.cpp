#include<iostream>
#include<stdio.h>
#include<string>
using namespace std;
int main()
{
	long long int n,l,answer;
	char c;
	scanf("%lld",&n);
	string seq;
	while(n--){
		cin>>seq;
		for(l=0;seq[l]!='\0';l++){
		if(seq[l]=='+')
		
		}
	}

return 0;
}
