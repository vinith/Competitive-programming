using namespace std;
#include<iostream>
#include<cstdio>
#include<cstdlib>

#define min(a,b) ((a)<(b)?(a):(b))
#define max(a,b) ((a)<(b)?(b):(a))
int main()
{
  int n;
  int path[110][110];
  scanf("%d",&n);
  for(int i=0;i<n;i++)for(int k=0;k<n;k++)scanf("%d",&path[i][k]);
  for(int i=n-2;i>-1;i--){
    
  }
  return 0;
}
